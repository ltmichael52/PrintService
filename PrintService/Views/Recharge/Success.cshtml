@{
    ViewData["Title"] = "Thanh Toán Thành Công";
}

<h2>@ViewData["Title"]</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success">
        @TempData["Message"]
    </div>
}

<p>Cảm ơn bạn đã sử dụng dịch vụ của chúng tôi! Giao dịch của bạn đã được hoàn tất.</p>

<a href="@Url.Action("Index", "Payment")" class="btn btn-primary">Quay lại trang nạp tiền</a>
